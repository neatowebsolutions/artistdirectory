import Head from 'next/head';
import { Layout } from '../../components';

const TagsPage = () => (
  <>
    <Head>
      <title>Tags</title>
    </Head>
    <Layout>List of tags here</Layout>
  </>
);

export default TagsPage;
