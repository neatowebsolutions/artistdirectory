import Head from 'next/head';
import { Layout } from '../components';

const CreatePage = () => (
  <>
    <Head>
      <title>Create Profile</title>
    </Head>
    <Layout>Profile creation form here</Layout>
  </>
);

export default CreatePage;
